```java
mLocationManager.registerGnssStatusCallback(new GnssStatus.Callback() {
            @Override
            public void onStarted() {
                Log.i(TAG, "registerGnssStatusCallback: onStarted");
            }

            @Override
            public void onStopped() {
                Log.i(TAG, "registerGnssStatusCallback: onStopped");
            }

            @Override
            public void onFirstFix(int ttffMillis) {
                Log.i(TAG, "registerGnssStatusCallback: onFirstFix " + ttffMillis);
            }

            @Override
            public void onSatelliteStatusChanged(@NonNull GnssStatus status) {
                Log.i(TAG, "registerGnssStatusCallback: onSatelliteStatusChanged " + status.getSatelliteCount());
            }
        });
```

基于安卓12

onSatelliteStatusChanged的调用链路如下：

```
listener.onSatelliteStatusChanged(gnssStatus) 
<-LocationManager#GnssStatusTransport.onSvStatusChanged
<-GnssStatusProvider.onReportSvStatus
<- GnssNative.reportSvStatus
<- com_android_server_location_GnssLocationProvider.cpp gnssSvStatusCbImpl 通过jni
<- com_android_server_location_GnssLocationProvider GnssCallback.gnssSvStatusCb

下面进入gnss hal 服务内部：
GpsCallbacks Gnss::sGnssCb = {
    .size = sizeof(GpsCallbacks),
    。。。。。省略
    .gnss_sv_status_cb = gnssSvStatusCb,
};

gnssSvStatusCb调用来自gnssSvStatusCb和gpsSvStatusCb，各家自己适配

```



onLocationChanged的调用链路

```
注册
mLocationManager.requestLocationUpdates("gps", 1000, 0, new LocationListener() {
。。。。
}
LocationManager里将provider, minTimeMs, minDistanceM封装成LocationRequest对象，LocationRequest是Parcelable
进入LocationManagerService：
mService.registerLocationListener(provider, locationRequest, transport,
                        mContext.getPackageName(), mContext.getAttributionTag(),
                        AppOpsManager.toReceiverId(listener));
                        
LocationProviderManager.registerLocationRequest(request, identity, permissionLevel, listener);
(request, identity)封装成 LocationListenerRegistration 对象
LocationProviderManager.putRegistration(listener.asBinder(), registration);

//LocationListener不是Parcelable，注册到LocationManagerService的是LocationListenerTransport extends ILocationListener.Stub
LocationManagerService收到listener后包装成另一个LocationListenerTransport对象：
        //参数是上面的LocationListenerTransport，这个LocationListenerTransport是syster_server里的另一个类，同名
        protected LocationListenerTransport(ILocationListener listener) {
            mListener = Objects.requireNonNull(listener);
        }
```

回调

```
调用链
gnss hal通过gnssLocationCb上报->
com_android_server_location_GnssLocationProvider.cpp GnssCallback::gnssLocationCb实现->
通过jni进入GnssNative.reportLocation ->
GnssLocationProvider.onReportLocation->
sendMessage(REPORT_LOCATION)进入handleReportLocation->
reportLocation(继承自AbstractLocationProvider)-》
listener.onReportLocation(listener是LocationProviderManager实例)->
....()->
LocationListenerTransport.deliverOnLocationChanged（注意是非Parcelable的那个LocationListenerTransport）
->
mListener.onLocationChanged（mListener是Parcelable的LocationListenerTransport）
-》下面就进入app端进程(oneway)
LocationListenerTransport.onLocationChanged
->经一个mExecutor转发listener.onLocationChanged(locations);//这个listener就是最开始app里面requestLocationUpdates传入的
->app收到onLocationChanged


//LocationProviderManager onReportLocation
...一些逻辑...
deliverToListeners(registration -> {
    //acceptLocationChange里检查 minTimeMs, minDistanceM，决定要不要给应用回调
    return registration.acceptLocationChange(filtered);//返回ListenerOperation对象
});

return new ListenerOperation<LocationTransport>() {
	public void operate(LocationTransport listener) throws Exception {
	   // listener是LocationListenerTransport或其它子类
        listener.deliverOnLocationChanged(deliverLocationResult,
        mUseWakeLock ? mWakeLockReleaser : null);
	}
}
```

