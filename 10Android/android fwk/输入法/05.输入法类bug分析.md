* InputMethodService

  InputMethodService运行在输入法应用进程

* InputMethodManager

  InputMethodManager运行在显示输入框的app的进程

* InputMethodManagerService

  InputMethodManagerService运行在system_server

调试时打开日志：

```
//InputMethodService、InputMethodManager、InputMethodManagerService三个类打开Debug开关
static final boolean  DEBUG = true;// false -> true
//如果设置了persist.log.tag，调整为V以便显示V级别的日志。

logcat | grep -E "InputMethodService|InputMethodManager|InputMethodManagerService"
```

